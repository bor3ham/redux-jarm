import getStore from './store.js'
import jarm from './jarm.js'

test('placeholder', () => {
  const store = getStore()
})

// update single attribute on existing instance
// update single relationship on existing instance
// update a committed instance (not pending)
// update a committed instance (pending)
// update an instance marked for deletion
// update an instance that does not exist
