import getStore from '../store.js'
import jarm from '../jarm.js'

test('placeholder', () => {
  const store = getStore()
})

// todo: flush uncommitted draft
// todo: flush uncommitted modified
// todo: flush uncommitted draft
// todo: flush uncommitted modified
// todo: flush pending draft
// todo: flush pending modified
