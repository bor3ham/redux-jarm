global.fetch = require('jest-fetch-mock')

// uncomment this line to catch uncaught promise rejections
// note that this will break jest when it actually catches an exception
// process.on('unhandledRejection', up => { throw up })
